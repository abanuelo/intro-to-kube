kind: RoleBinding
apiVersion: rbac.authorization.k8s.io/v1
metadata:
  name: write-pods
  namespace: default
subjects:
# TODO: add podwriter as target user
# note: this is case sensitive
roleRef:
# TODO: add target role to pod-writer
