apiVersion: v1
kind: ConfigMap
metadata:
  name: postgres-config
  labels:
    app: postgres
data:
  POSTGRES_DB: postgresdb
  POSTGRES_USER: admin
  POSTGRES_PASSWORD: psltest
  # TODO: Configure the following values
  # POSTGRES_DB as postgresdb
  # POSTGRES_USER as admin
  # POSTGRES_PASSWORD as psltest
