apiVersion: v1
kind: Pod
metadata:
  name: nginx-pod
spec:
  containers:
  - name: just-a-pod
    image: nginx:latest
    ports:
    - containerPort: 80

  # TODO: 
  # - Add your nginx-pod spec with label `just-a-pod` and containerPort 80